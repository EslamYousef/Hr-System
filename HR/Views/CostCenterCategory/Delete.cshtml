@model HR.Models.SetupPayroll.CostCenterCategory

@using HR.Resource;

@{
    ViewBag.Title = "Delete";
    Layout = "~/Views/Shared/MainLayOut.cshtml";
}


<h3>@Basic.message_delete</h3>
@using (Html.BeginForm("Delete", "CostCenterCategory", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    @Html.AntiForgeryToken()
    <div class="form-horizontal">

        <hr />

        @if (TempData["Message"] != null)
        {
            <div class="alert alert-danger alert-dismissible" role="alert">
                <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                @TempData["Message"]
            </div>
        }


        <div class="form-group">
            <label class="control-label col-md-2">
                @Payroll.CategoryCode
            </label>
            <div class="col-md-8">
                @Html.EditorFor(model => model.CategoryCode, new { htmlAttributes = new { @class = "form-control", @readonly = true } })

            </div>
        </div>

        <div class="form-group">
            <label class="control-label col-md-2">
                @Payroll.Description
            </label> <div class="col-md-8">
                @Html.EditorFor(model => model.CategoryDesc, new { htmlAttributes = new { @class = "form-control", @readonly = true } })
            </div>
        </div>
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <a href="@Url.Action("index","CostCenterCategory")" class="btn btn-danger btn-sm"><i class="fa fa-trash-o"></i>@Basic.Back</a>
                <input type="submit" value=@Basic.Delete class="btn btn-success" />

            </div>
        </div>
    </div>
}

@section scripts{}