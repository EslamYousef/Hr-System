@model IEnumerable<HR.Models.Subscription_Syndicate>
@using HR.Resource;
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/MainLayOut.cshtml";
}

<div class="content-wrapper">
    <!-- Main content -->
    <section class="content">

        <div class="box">
            <div class="box-header">
                <p class="lead">
                    <a href="@Url.Action("Create", "Subscription_Syndicate")" class="btn btn-success btn-sm"><i class="fa fa-edit"></i>@Personnel.Add_New_Subscription_Syndicate</a>
                </p>
            </div>

            <!-- /.box-header -->
            <div class="box-body">
                @if (TempData["Message"] != null)
                {
                    <div class="alert alert-danger alert-dismissible" role="alert">
                        <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                        @TempData["Message"]
                    </div>
                }

                <table id="categories" class="table table-bordered table-striped text-center">
                    <thead>
                        <tr>
                            <th>@Personnel.Subscription_Code</th>
                            <th>@Personnel.Subscription_Description </th>
                            <th>@Personnel.Server_Legatees </th>
                            <th>@Personnel.Deduction_Period </th>
                            <th>@Personnel.Type</th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@item.Subscription_Code</td>
                                <td>@item.Subscription_Description</td>
                                <td>@item.Server_Legatees</td>
                                <td>@item.Deduction_Period</td>
                                <td>@item.Type</td>


                                <td>
                                    <a href="@Url.Action("Edit", "Subscription_Syndicate", new {id = item.ID})" class="btn btn-primary btn-sm" style="width:50px"><i class="fa fa-edit"></i>@Personnel.Edit</a>
                                    <a href="@Url.Action("delete", "Subscription_Syndicate", new {id = item.ID})" class="btn btn-danger btn-sm"><i class="fa fa-trash-o"></i>@Personnel.Delete</a>
                                </td>
                            </tr>
                        }
                    </tbody>
               
                </table>
            </div>
        </div>

    </section>

</div>
@section scripts{}

<!-- page script -->
<script>
    var $ = jQuery;

    $(document).ready(function () {
        $('#categories').DataTable({
            "paging": true,
            "lengthChange": true,
            "searching": true,
            "ordering": false,
            "info": true,
            "autoWidth": false,
            "order" : 1
        });
    });
</script>
