@model IEnumerable<HR.Models.ViewModel.Employee_Experience_VM>
@using HR.Resource;
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/MainLayOut.cshtml";
}

<div class="content-wrapper">
    <!-- Main content -->
    <section class="content">

        <div class="box">
            <div class="box-header">
                <p class="lead">
                    @Personnel.Employeeexperienceprofile
                </p>
            </div>

            <!-- /.box-header -->
            <div class="box-body">
                @if (TempData["Message"] != null)
                {
                    <div class="alert alert-success alert-dismissible" role="alert">
                        <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                        @TempData["Message"]
                    </div>
                }

                <table id="categories" class="table table-bordered table-striped text-center">
                    <thead>
                        <tr>
                            <th>@Personnel.ExperienceprofileNo</th>         
                            <th>@Personnel.EmployeeName</th>
                            <th>@Personnel.Companycode</th>
                            <th>@Personnel.Companytype</th>
                            <th>@Personnel.Jobtitle</th>
                            <th>@Personnel.Reasonofleave</th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>




                        @foreach (var item in Model)
                        {
                            <tr>

                                <td>@item.Employee_experience_profile.Code</td>
                                <td>@item.fullname</td>
                                @if (@item.Employee_experience_profile.External_compaines !=null)
                                {
                                    <td>@item.Employee_experience_profile.External_compaines.Name</td>
                                }
                                else
                                {
                                    <td>Empty</td>
                                }
                                @if (@item.Employee_experience_profile.Company_type != null)
                                {
                                <td>@item.Employee_experience_profile.Company_type</td>
                                }
                                else{<td>Empty</td>}
                                @if (@item.Employee_experience_profile.Job_title != null)
                                {
                                    <td>@item.Employee_experience_profile.Job_title</td>
                                }
                                else
                                {
                                <td>Empty</td>
                                }
                                @if (@item.Employee_experience_profile.Rejection_Reasons != null)
                                {
                                    <td>@item.Employee_experience_profile.Rejection_Reasons.Name</td> }
                                else
                                {
                                    <td>Empty</td> }
                                <td>
                                    <a href="@Url.Action("Edit", "Employee_experience_profile", new {id = item.Employee_experience_profile.ID})" class="btn btn-primary btn-sm" style="width:50px"><i class="fa fa-edit"></i>@Personnel.Edit1</a>
                                    @*<a href="@Url.Action("delete", "Employee_Address_Profile", new {id = item.Employee_Address_Profile.ID})" class="btn btn-danger btn-sm"><i class="fa fa-trash-o"></i>Delete</a>*@
                                </td>
                            </tr>
                        }


                    </tbody>
           
                </table>
            </div>
        </div>

    </section>

</div>

<!-- page script -->
<script>
    var $ = jQuery;

    $(document).ready(function () {
        $('#categories').DataTable({
            "paging": true,
            "lengthChange": true,
            "searching": true,
            "ordering": false,
            "info": true,
            "autoWidth": false,
            "order" : 1
        });
    });
</script>

@section scripts{}