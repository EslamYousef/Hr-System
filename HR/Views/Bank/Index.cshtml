@model IEnumerable<HR.Models.SetupPayroll.Bank>

@using HR.Resource;

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/MainLayOut.cshtml";
}

<div class="content-wrapper">
    <!-- Main content -->
    <section class="content">

        <div class="box">
            <div class="box-header">
                <p class="lead">
                    <a href="@Url.Action("Create", "Bank")" class="btn btn-success btn-sm"><i class="fa fa-edit"></i>@Payroll.AddNewBank</a>
                </p>
            </div>

            <!-- /.box-header -->
            <div class="box-body">
                @if (TempData["Message"] != null)
                {
                    <div class="alert alert-success alert-dismissible" role="alert">
                        <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                        @TempData["Message"]
                    </div>
                }

                <table id="categories" class="table table-bordered table-striped text-center">
                    <thead>
                        <tr>
                            <th>@Payroll.Code</th>
                            <th>@Payroll.Description</th>
                            <th>@Payroll.AlternativeDescription</th>
                            <th>@Payroll.Swiftnumber</th>

                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                        <tr>
                            <td>@item.Bank_Code</td>
                            <td>@item.Bank_Desc</td>
                            <td>@item.Bank_AltDesc</td>
                            <td>@item.Swift_Number</td>

                            <td>
                                <a href="@Url.Action("Edit", "Bank", new {id = item.ID})" class="btn btn-primary btn-sm" style="width:50px"><i class="fa fa-edit"></i>@organ.change</a>
                                <a href="@Url.Action("Delete", "Bank", new {id = item.ID})" class="btn btn-danger btn-sm"><i class="fa fa-trash-o"></i>@Basic.Delete</a>
                            </td>
                        </tr>
                        }
                    </tbody>

                </table>
            </div>
        </div>

    </section>

</div>

<!-- page script -->s
<script>
    var $ = jQuery;

    $(document).ready(function () {
        $('#categories').DataTable({
            "paging": true,
            "lengthChange": true,
            "searching": true,
            "ordering": false,
            "info": true,
            "autoWidth": false,
            "order" : 1
        });
    });
</script>

@section scripts{}