@model IEnumerable<HR.Models.Employee_Profile>
@using HR.Resource;
@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/MainLayOut.cshtml";

}


@Scripts.Render("~/bundles/jqueryval")
<div class="content-wrapper">

    <section class="content-header">
        <h1>
            @Personnel.EditApplication
        </h1>
    </section>

    <!-- Main content -->
    <section class="content">

        <div class="box">

            <!-- /.box-header -->
            <div class="box-body">

                @using (Html.BeginForm("Edit", "EditAssign", FormMethod.Post, new { enctype = "multipart/form-data" }))
                {
                    @Html.AntiForgeryToken()
                    <div class="form-horizontal">

                        <hr />

                        @if (TempData["Message"] != null)
                        {
                            <div class="alert alert-danger alert-dismissible" role="alert">
                                <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                                @TempData["Message"]
                            </div>
                        }

                        @Html.ValidationSummary(true, "", new { @class = "text-danger" })




                        <div class="col-md-3" style="color:black;margin-bottom:10px;">
                            <a href="@Url.Action("index","salary_code")" class="btn btn-danger btn-sm" style="width:98px;"><i class="fa fa-trash-o"></i>@Personnel.Back</a>
                            <input type="submit" value="@Personnel.Edit" class="btn btn-success" style="width:98px;" />
                        </div>



                        <div>
                            <ul class="nav nav-tabs bordered">
                                <!-- available classes "bordered", "right-aligned" -->
                                <li class="active">
                                    <a href="#home" data-toggle="tab">
                                        <span class="visible-xs"><i class="entypo-home"></i></span>
                                        <span class="hidden-xs">@Personnel.Home</span>
                                    </a>
                                </li>

                            </ul>
                            <div class="tab-content">
                                <div class="tab-pane active" id="home" style="overflow: hidden;width: auto;height: auto;">


                                    @*<div class="form-group">
                                            <label class="control-label col-md-1">salarycode</label>
                                            <div class="col-md-1">
                                                @Html.EditorFor(model => model.salary_code.SalaryCodeID, new { htmlAttributes = new { @class = "form-control", @readonly = true } })
                                                @Html.ValidationMessageFor(model => model.salary_code.SalaryCodeID, "", new { @class = "text-danger" })
                                            </div>

                                        </div>
                                        <div class="form-group">
                                            <label class="control-label col-md-1">
                                                <span> @pers_2.codetype </span>
                                            </label>
                                            <div class="col-md-3">
                                                @Html.EnumDropDownListFor(m => m.code_type, pers_2.codetype, new { @class = "select2", @id = "Country1" })
                                            </div>

                                            <label class="control-label col-md-1">
                                                <span> @pers_2.codevaluetype </span>
                                            </label>
                                            <div class="col-md-3">
                                                @Html.EnumDropDownListFor(m => m.code_value_type, pers_2.codevaluetype, new { @class = "select2", @id = "Country1" })
                                            </div>
                                        </div>*@
                                    <div>
                                        <table id="categories" class="table table-bordered table-striped text-center">
                                            <thead>
                                                <tr>
                                                    <th>@pers_2.salarycodeid</th>
                                                    <th>@organ.name</th>
                                                    <th></th>
                                                </tr>
                                            </thead>
                                            <tbody>
                                                @foreach (var item in Model)
                                                {
                                                    <tr>
                                                        <td>@item.Code</td>
                                                        <td>@item.Full_Name</td>
                                                    </tr>
                                                }
                                            </tbody>

                                        </table>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                }
            </div>
        </div>
    </section>
</div>
@section scripts{}



<script>
    $(function () {
        $("#employeeId8").attr("disabled", true);

        function employee() {
            debugger
            var id = $("#employeeId8").val();
            $.ajax({
                type: "post",
                url: "/Helper/GetApplicant",
                data: { "id": id },
                async: false,
                success: function (result) {
                    debugger
                    $("#full").val(result.Full_Name);
                    $("#SurEn").val(result.Surname);
                    $("#full2").val(result.Full);
                    $("#SurAr").val(result.Sur_Name);
                    var FullName = $("#full").val().split(' ');
                    if (FullName.length > 0) {
                        $("#name1").val(FullName[0]);
                        $("#name2").val(FullName[1]);
                        $("#name3").val(FullName[2]);
                        $("#name4").val(FullName[3]);
                        $("#name5").val(FullName[4]);
                    }
                    var FullName2 = $("#full2").val().split(' ');
                    if (FullName2.length > 0) {
                        $("#arabic1").val(FullName2[0]);
                        $("#arabic2").val(FullName2[1]);
                        $("#arabic3").val(FullName2[2]);
                        $("#arabic4").val(FullName2[3]);
                        $("#arabic5").val(FullName2[4]);
                    }
                },
                error: function () {

                }
            });
        }
        employee();
        function GetDataById2(date, id) {
            debugger
            var valx = new Date(date);
            var month;
            if ((valx.getMonth() + 1) < 10) {
                month = "0" + (valx.getMonth() + 1).toString();
            }
            else {
                month = date.getMonth() + 1;
            }
            var day = parseInt(valx.getDate());
            if (day < 10) {
                day = "0" + (valx.getDate()).toString();
            }
            else {
                day = valx.getDate();
            }
            var d = valx.getFullYear() + '-' + month + '-' + day;
            $('#' + id).val(d);
        }
    })
    function Religion() {

        var id = $("#religion").val();
        $.ajax({
            type: "post",
            url: "/Helper/GetCommitteResolutionRecuirtment",
            data: { "id": id },
            success: function (result) {

                $("#ReligionName").val(result.Code);
            },
            error: function () {

                $("#ReligionName").val(" ");
            }
        });
    }

</script>


