@model IEnumerable<HR.Models.TicketPrice>
@using HR.Resource;

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/MainLayOut.cshtml";
}

<div class="content-wrapper">
    <!-- Main content -->
    <section class="content">

        <div class="box">
            <div class="box-header">
                <p class="lead">
                    <a href="@Url.Action("Create", "Ticket_Prices")" class="btn btn-success btn-sm"><i class="fa fa-edit"></i>@Basic.add_new_ticket_price</a>
                </p>
            </div>

            <!-- /.box-header -->
            <div class="box-body">
                @if (TempData["Message"] != null)
                {
                    <div class="alert alert-success alert-dismissible" role="alert">
                        <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                        @TempData["Message"]
                    </div>
                }

                <table id="categories" class="table table-bordered table-striped text-center">
                    <thead>
                        <tr>
                            <th>@Basic.class_type</th>
                            <th>@Basic.from_air_port</th>
                            <th>@Basic.to_air_port</th>
                            <th>@Basic.from_date</th>
                            <th>@Basic.to_date</th>
                            <th>@Basic.price</th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@item.classtype</td>
                                <td>@item.From_Air_port.Name</td>
                                <td>@item.To_Air_port.Name</td>
                                <td>@item.From_Date</td>
                                <td>@item.TO_Date</td>
                                <td>@item.Price</td>
                                <td>
                                    <a href="@Url.Action("Edit", "Ticket_Prices", new {id = item.ID})" class="btn btn-primary btn-sm" style="width:50px"><i class="fa fa-edit"></i>@Basic.Edit</a>
                                    <a href="@Url.Action("delete", "Ticket_Prices", new {id = item.ID})" class="btn btn-danger btn-sm"><i class="fa fa-trash-o"></i>@Basic.Delete</a>
                                </td>
                            </tr>
                        }
                    </tbody>
                    <tfoot>
                        <tr>

                            <th>@Basic.class_type</th>
                            <th>@Basic.from_air_port</th>
                            <th>@Basic.to_air_port</th>
                            <th>@Basic.from_date</th>
                            <th>@Basic.to_date</th>
                            <th>@Basic.price</th>

                            <th></th>
                        </tr>
                    </tfoot>
                </table>
            </div>
        </div>

    </section>

</div>

<!-- page script -->
<script>
    var $ = jQuery;

    $(document).ready(function () {
        $('#categories').DataTable({
            "paging": true,
            "lengthChange": true,
            "searching": true,
            "ordering": false,
            "info": true,
            "autoWidth": false,
            "order" : 1
        });
    });
</script>

@section scripts{}